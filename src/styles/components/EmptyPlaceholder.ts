import { StyleSheet } from 'react-native';

export const EmptyPlaceholderStyles = StyleSheet.create({
  messageContainer: {
    alignItems: 'center',
    justifyContent: 'center',
  },
  messageText: {
    fontSize: 30,
    fontWeight: 'bold',
  },
});
